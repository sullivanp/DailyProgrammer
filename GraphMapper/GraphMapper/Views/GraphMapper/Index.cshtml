@model IEnumerable<GraphMapper.Models.Grid>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rows)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cols)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rows)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cols)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GridID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GridID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GridID })
        </td>
    </tr>
}

</table>
